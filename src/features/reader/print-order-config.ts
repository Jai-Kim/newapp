/**
 * Concierge print pricing (issue #22, ADR-0003, slice 4).
 *
 * Indicative only — there is no payment collection in this slice, so this
 * number is shown as "about $X", never charged. #14 mentions ~$45; the exact
 * customer-facing price and refund/cancellation wording are open questions
 * for Jai, not invented here.
 */
export const INDICATIVE_HARDCOVER_PRICE_USD = 45;
